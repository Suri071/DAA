# include<stdio.h>
int main()
{
int sum = 0,n,r;
printf("Enter an integer: ");
scanf("%d"
, &n);
while (n != 0)
{
r=n%10;
sum = sum + r;
n = n/10;
}
Design Analysis and Algorithms
printf(" SUM= %d "
, sum);
}
